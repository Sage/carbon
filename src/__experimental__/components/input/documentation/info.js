import React from "react";
import { StoryHeader } from "../../../../../.storybook/style/storybook-info.styles";

const info = (
  <div>
    <StoryHeader>Input Integration Story</StoryHeader>
    <p>
      This story is intended for testing the correct functioning of all
      input-based components in one place.
    </p>
  </div>
);

export default info;
