import styled from 'styled-components';

const StyledSimpleColorPicker = styled.div`
  width: 300px;
`;

const StyledColorOptions = styled.ul`
  padding: 0;
`;

export { StyledSimpleColorPicker, StyledColorOptions };
