/**
 * Defines the string types of items
 * that can be dragged and dropped.
 *
 * NB drag sources and drop targets only interact
 * if they have the same string type.
 */
const ItemTypes = {
  TABLE_ROW: 'TableRow'
};

export default ItemTypes;
