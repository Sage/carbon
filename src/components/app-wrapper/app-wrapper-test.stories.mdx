import { Meta, Story, Canvas } from "@storybook/addon-docs";

import specialCharacters from "../../../.storybook/utils/argTypes/specialCharacters";
import AppWrapper from "./app-wrapper.component";

<Meta
  title="AppWrapper/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    childrenSpecialCharacters: specialCharacters,
  }}
/>

export const AppWrapperStory = ({
  children,
  childrenSpecialCharacters,
  ...args
}) => <AppWrapper children={children || childrenSpecialCharacters} {...args} />;

# Alert Wrapper

### Default

<Canvas>
  <Story
    name="default"
    args={{
      children:
        "This component will wrap its children within the width constraints of your application.",
      childrenSpecialCharacters: undefined,
    }}
  >
    {AppWrapperStory.bind({})}
  </Story>
</Canvas>
