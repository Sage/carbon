import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import AppWrapper from "./app-wrapper.component";

<Meta
  title="AppWrapper/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
    knobs: {
      disable: true,
    },
  }}
/>

export const AppWrapperStory = (args) => <AppWrapper {...args} />;

# Alert Wrapper

### Default

<Preview>
  <Story
    name="default"
    args={{
      children:
        "This component will wrap its children within the width constraints of your application.",
    }}
  >
    {AppWrapperStory.bind({})}
  </Story>
</Preview>
