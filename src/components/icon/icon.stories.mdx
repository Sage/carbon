import { Meta, Story, Preview } from "@storybook/addon-docs";
import LinkTo from "@storybook/addon-links/react";
import StyledSystemProps from "../../../.storybook/utils/styled-system-props";

import Icon from "./";
import Box from "../box";

<Meta
  title="Icon"
  parameters={{ info: { disable: true }, chromatic: { disable: true } }}
/>

# Icon

Carbon comes with more than 100 standard icons to choose from. See the Icons page in the Style section.

A tooltip option is available within this component.

Many other components allow you to specify one of the standard Carbon icons to associate with them, for example, the Link component.

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Props](#props)

## Quick Start

```javascript
import Icon from "carbon-react/lib/components/icon";
```

## Examples

### Default

<Preview>
  <Story name="default">
    <Icon type="add" />
  </Story>
</Preview>

### Disabled

<Preview>
  <Story name="disabled">
    <Icon type="add" disabled />
  </Story>
</Preview>

### With tooltip

The Icon supports rendering tooltips internally, just pass a node to `tooltipMessage` to enable this feature. Other props,
such as `tooltipPosition`, `tooltipVisible` , `tooltipBgColor`, `tooltipFontColor` and `tooltipFlipOverrides` are also surfaced.

<Preview>
  <Story name="with tooltip">
    <div style={{ margin: 60, display: "inline" }}>
      <Icon mr={8} type="add" tooltipMessage="Hey I'm a default tooltip!" />
      <Icon
        mr={8}
        type="add"
        tooltipMessage={
          <>
            Hey I'm a <em>custom</em> tooltip!
          </>
        }
      />
      <Icon
        mr={8}
        type="add"
        tooltipMessage="Hey I'm a tooltip with a different position!"
        tooltipPosition="bottom"
      />
      <Icon
        mr={8}
        type="add"
        tooltipMessage="Hey I'm a tooltip with a different color!"
        tooltipBgColor="lightblue"
        tooltipFontColor="black"
      />
      <Icon
        type="add"
        tooltipMessage="Hey I'm a tooltip with flip behaviour overrides!"
        tooltipFlipOverrides={["right", "left"]}
      />
    </div>
  </Story>
</Preview>

### Various fontSizes

<Preview>
  <Story name="various fontSizes">
    {["small", "medium", "large", "extra-large"].map((fontSize) => (
      <Icon type="add" fontSize={fontSize} key={fontSize} />
    ))}
  </Story>
</Preview>

### Various bgThemes

<Preview>
  <Story name="various bgThemes">
    {["info", "error", "success", "warning", "business", "none"].map(
      (bgTheme) => (
        <Icon type="add" bgTheme={bgTheme} mr={1} key={bgTheme} />
      )
    )}
  </Story>
</Preview>

### Various bgShapes

<Preview>
  <Story name="various bgShapes">
    {["circle", "rounded-rect", "square"].map((bgShape) => (
      <Icon
        type="add"
        bgTheme="success"
        bgShape={bgShape}
        mr={1}
        key={bgShape}
      />
    ))}
  </Story>
</Preview>

### Various bgSizes

<Preview>
  <Story name="various bgSizes">
    {["small", "medium", "large", "extra-large"].map((bgSize) => (
      <Icon type="add" bgTheme="success" bgSize={bgSize} mr={1} key={bgSize} />
    ))}
  </Story>
</Preview>

### IconColor variants

<Preview>
  <Story name="iconColor variants">
    {[
      "default",
      "on-light-background",
      "on-dark-background",
      "business-color",
    ].map((iconColor) => (
      <Icon type="add" iconColor={iconColor} mr={1} key={iconColor} />
    ))}
  </Story>
</Preview>

### Custom colors

It's possible to change the color of the Icon by using the `color` prop or background color by `bg` prop.
These props will override `iconColor` and `bgTheme` prop values.
See <LinkTo kind="Documentation/Colors" name="page" story="page">Colors</LinkTo> for more information.

<Preview>
  <Story
    name="custom colors"
    parameters={{ info: { disable: true }, chromatic: { disable: true } }}
  >
    <>
      <Box mb={1}>
        <Icon type="add" color="primary" />
        <Icon type="add" color="blackOpacity65" />
        <Icon type="add" color="brilliantGreenShade20" />
        <Icon type="add" color="red" />
        <Icon type="add" color="#123456" />
        <Icon type="add" color="rgb(0, 123, 10)" />
      </Box>
      <Box mb={1}>
        <Icon type="add" color="red" bg="primary" />
        <Icon type="add" color="white" bg="blackOpacity65" />
        <Icon type="add" bg="brilliantGreenShade20" />
        <Icon type="add" bg="red" />
        <Icon type="add" color="white" bg="#123456" />
        <Icon type="add" color="white" bg="rgb(0, 123, 10)" />
      </Box>
    </>
  </Story>
</Preview>

## Props

### Icon

<StyledSystemProps of={Icon} noHeader margin />
