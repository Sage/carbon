import { Meta, Story, Canvas } from "@storybook/addon-docs";

import specialCharacters from "../../../.storybook/utils/argTypes/specialCharacters";
import SettingsRow from ".";

<Meta
  title="Setting Row/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    childrenSpecialCharacters: specialCharacters,
    descriptionSpecialCharacters: specialCharacters,
    titleSpecialCharacters: specialCharacters,
  }}
/>

export const SettingsRowStory = ({
  childrenSpecialCharacters,
  children,
  descriptionSpecialCharacters,
  description,
  titleSpecialCharacters,
  title,
  ...args
}) => (
  <SettingsRow
    children={children || childrenSpecialCharacters}
    description={description || descriptionSpecialCharacters}
    title={title || titleSpecialCharacters}
    {...args}
  />
);

# Settings Row

### Default

<Canvas>
  <Story
    name="default"
    args={{
      children: "Content for settings",
      description:
        "This provides more information about what this group of settings are for.",
      divider: true,
      title: "A GROUP OF SETTINGS",
      childrenSpecialCharacters: undefined,
      descriptionSpecialCharacters: undefined,
      titleSpecialCharacters: undefined,
    }}
  >
    {SettingsRowStory.bind({})}
  </Story>
</Canvas>
