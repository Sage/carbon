import { Meta, Story, Canvas } from "@storybook/addon-docs";

import specialCharacters from "../../../.storybook/utils/argTypes/specialCharacters";
import NavigationBar from "./navigation-bar.component";

<Meta
  title="Navigation Bar/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    childrenSpecialCharacters: specialCharacters,
  }}
/>

export const NavigationBarStory = ({
  children,
  childrenSpecialCharacters,
  ...args
}) => (
  <NavigationBar children={children || childrenSpecialCharacters} {...args} />
);

# Navigation Bar

### Default

<Canvas>
  <Story
    name="default"
    args={{
      children: "Example content",
      childrenSpecialCharacters: undefined,
    }}
  >
    {NavigationBarStory.bind({})}
  </Story>
</Canvas>
