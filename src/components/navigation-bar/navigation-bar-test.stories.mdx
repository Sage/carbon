import { Meta, Story, Canvas } from "@storybook/addon-docs";
import NavigationBar from "./navigation-bar.component";

<Meta
  title="Navigation Bar/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disableSnapshot: true,
    },
  }}
/>

export const NavigationBarStory = ({
  children,
  ...args
}) => (
  <NavigationBar children={children} {...args} />
);

# Navigation Bar

### Default

<Canvas>
  <Story
    name="default"
    args={{
      children: "Example content",
    }}
  >
    {NavigationBarStory.bind({})}
  </Story>
</Canvas>
