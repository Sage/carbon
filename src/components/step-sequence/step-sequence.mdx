import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";
import * as StepSequenceStories from "./step-sequence.stories.tsx";
import * as StepSequenceItemStories from "./step-sequence-item/step-sequence-item.stories.tsx";

<Meta title="Step Sequence" of={StepSequenceStories} />

# Step Sequence

<a
  target="_blank"
  href="https://zeroheight.com/2ccf2b601/p/405a47-step-flow"
  style={{ color: "#007E45", fontWeight: "bold", textDecoration: "underline" }}
  rel="noreferrer"
>
  Product Design System component
</a>

Indicate the progress of a step flow.

Progress indicators always directly mirror the number of pages, not general categories.

Try to keep label text for each step as short as possible.

For users on small screens or instances where horizontal space is limited, use the vertical version of this component.

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Props](#props)

## Quick Start

```javascript
import {
  StepSequence,
  StepSequenceItem,
} from "carbon-react/lib/components/step-sequence";
```

## Examples

### Default

The `StepSequence` component defaults to vertical orientation.

<Canvas of={StepSequenceStories.DefaultStory} />

### Horizontal

To orient `StepSequence` horizontally, use the `orientation` props.

<Canvas of={StepSequenceStories.Horizontal} />

### Responsive

The `StepSequence` component does not natively respond to screen size changes. The following demonstrates a method of swapping between orientations according to device size.

<Canvas of={StepSequenceStories.Responsive} />

### Size

`StepSequence` has two sizes available: **small** and **medium**. By default, **medium** is used if no value is set. The following example demonstrates the **small** variation.

<Canvas of={StepSequenceStories.Size} />

## Props

### StepSequence

<ArgTypes of={StepSequenceStories} />

### StepSequenceItem

<ArgTypes of={StepSequenceItemStories} />
