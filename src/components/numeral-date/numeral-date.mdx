import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";
import TranslationKeysTable from "../../../.storybook/utils/translation-keys-table";

import * as NumeralDateStories from "./numeral-date.stories";

<Meta of={NumeralDateStories} />

# Numeral Date

For dates far from today, use this Numeral Date component. It is advised to use three inputs consisting of day, month, and year.
For dates close to today, we advise the use of a standard datepicker. If you require this, please see the "Date Input" component.

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Validation states](#validation-states)
- [Props](#props)
- [Translation keys](#translation-keys)
- [Ref methods](#ref-methods)

## Quick Start

```javascript
import NumeralDate, { NumeralDateProps, type NumeralDateHandle } from "carbon-react/lib/components/numeral-date";
```

## Examples

### Default

<Canvas of={NumeralDateStories.Default} />

### With Legend Hint

Keep legend hint text to a single short sentence, ensuring that any example dates you use are valid for the component.

<Canvas of={NumeralDateStories.WithLegendHint} />

### Date Formats

By default, the `NumeralDate` component uses the `dd/mm/yyyy` format. You can change this by using the `dateFormat` prop. 
The available formats are: 

<Canvas of={NumeralDateStories.DateFormats} />

### Validating date values

`NumeralDate` supports standard validation via the `error` prop, which can be used to display an error message when the date value is invalid.

However, `NumeralDate` also has a built-in feature to validate date values automatically to ensure they are within valid ranges, so that you don't have to recreate this logic yourself.
This can be enabled via the `enableInternalError` prop. The rendered messages are displayed alongside the messages defined with the regular `error` prop.

This in-built validation feature uses translation keys for the invalid date messages, so you can modify them to suit your localisation needs. 
The keys are `numeralDate.validation.day`, `numeralDate.validation.month`, and `numeralDate.validation.year`.

**Note**: This component does not support isolated input validation. An error in any single field will cause the entire component - including valid fields - to display an error state.

<Canvas of={NumeralDateStories.InternalValidationError} />

### Size

You can use the `size` prop to set the size of the field.

<Canvas of={NumeralDateStories.Size} />

### Required

<Canvas of={NumeralDateStories.Required} />

### Read-Only

<Canvas of={NumeralDateStories.ReadOnly} />

### Disabled

<Canvas of={NumeralDateStories.Disabled} />

### Programmatic Focus

The `NumeralDateHandle` type provides an imperative handle for programmatic control over `NumeralDate`. 
Using a `ref`, you can access its `focus()` method to set focus on the first input as needed.

<Canvas of={NumeralDateStories.ProgrammaticFocus} />

### With Custom Field IDs

It is possible to set custom IDs for each of the field inputs using the prop `inputIds`:

<Canvas of={NumeralDateStories.WithCustomFieldIds} />

## Validation States

This component supports input validation, see our [Validations](../?path=/docs/documentation-validations--docs) documentation page for more information.

## Props

### NumeralDate

<ArgTypes of={NumeralDateStories} />

## Translation keys

The following keys are available to override the translations for this component by passing in a custom locale object to the
[i18nProvider](../?path=/docs/documentation-i18n--docs).

<TranslationKeysTable
  translationData={[
    {
      name: "numeralDate.validation.day",
      description:
        "The error text displayed if the day input value is not valid.",
      type: "func",
      returnType: "string",
    },
    {
      name: "numeralDate.validation.month",
      description:
        "The error text displayed if the month input value is not valid.",
      type: "func",
      returnType: "string",
    },
    {
      name: "numeralDate.validation.year",
      description:
        "The error text displayed if the year input value is not valid.",
      type: "func",
      returnType: "string",
    },
    {
      name: "numeralDate.labels.day",
      description: "The text displayed within the day input label.",
      type: "func",
      returnType: "string",
    },
    {
      name: "numeralDate.labels.month",
      description: "The text displayed within the month input label.",
      type: "func",
      returnType: "string",
    },
    {
      name: "numeralDate.labels.year",
      description: "The text displayed within the year input label.",
      type: "func",
      returnType: "string",
    },
  ]}
/>

## Ref methods

`NumeralDate`'s forwarded ref exposes the following imperative methods:

| Method Name | Description                               |
| ----------- | ----------------------------------------- |
| `focus()`   | Programmatically focuses the first input. |
