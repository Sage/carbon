import { Meta, Story, Preview } from "@storybook/addon-docs";

import FileInput from ".";
import { LOADER_BAR_SIZES } from "../loader-bar/loader-bar.config";
import LoaderBar from "../loader-bar";

<Meta
  title="File Input/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    size: {
      options: ["small", "medium", "large"],
      control: {
        type: "select",
      },
    },
  }}
/>

export const FileInputStory = ({ size, ...args }) => (
  <FileInput fileChooseAction={() => {}} size={size} {...args} />
);

<Preview>
  <Story
    name="default"
    args={{
      size: "medium",
      disabled: false,
      error: "",
      isUploading: false,
      allowMultiple: false,
      draggable: false,
      dragPlaceholder: "",
      label: "File input",
    }}
  >
    {FileInputStory.bind({})}
  </Story>
</Preview>
