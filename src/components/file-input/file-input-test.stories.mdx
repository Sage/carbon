import { Meta, Story, Preview } from "@storybook/addon-docs";

import FileInput from ".";

<Meta
  title="File Input/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    size: {
      options: ["small", "medium", "large"],
      control: {
        type: "select",
      },
    },
    loaderType: {
      options: ["untracked", "tracked"],
      control: {
        type: "select",
      },
    },
  }}
/>

export const FileInputStory = ({ size, ...args }) => (
  <FileInput fileChooseAction={() => {}} size={size} {...args} />
);

<Preview>
  <Story
    name="default"
    args={{
      size: "medium",
      disabled: false,
      error: "",
      warning: "",
      isUploading: false,
      allowMultiple: false,
      draggable: false,
      dragPlaceholder: "",
      label: "File input",
      hintText: "",
      loaderType: "untracked",
      progress: 0,
      readOnly: false,
    }}
  >
    {FileInputStory.bind({})}
  </Story>
</Preview>
