import React from 'react';
import TestUtils from 'react/lib/ReactTestUtils';
import TableFieldsForMany from './index';

describe('TableFieldsForMany', () => {
  it('failing test demo', () => {
    expect(1).toEqual(2);
  });
});
