import { Meta, Story, Canvas } from "@storybook/addon-docs";
import Button from "../button";
import MinorButton from ".";
import StyledSystemProps from "../../../.storybook/utils/styled-system-props";

<Meta title="Minor Button" parameters={{ info: { disable: true } }} />

# Button

A Button triggers a single action or event.
Use it to submit a form (Save), to advance to the next step in a process (Next), or to create a new item (New).

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Props](#props)

## Quickstart

```javascript
import MinorButton from "carbon-react/lib/components/minor-button";
```

## Examples

### Minor Primary Buttons

For the single most prominent call to action on the page (e.g. Save, Submit, Continue).

<Canvas>
  <Story name="minor/primary">
    <>
      <MinorButton mt={2} ml={2} buttonType="primary"  size="small">
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Primary buttons can be destructive.

<Canvas>
  <Story name="minor/primary/destructive">
    <>
      <MinorButton mt={2} ml={2} buttonType="primary"  destructive size="small">
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  destructive ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  destructive size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Primary buttons can be disabled.

<Canvas>
  <Story name="minor/primary/disabled">
    <>
      <MinorButton mt={2} ml={2} buttonType="primary"  disabled size="small">
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  disabled ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="primary"  disabled size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Primary buttons can have an icon positioned before or after the text.

<Canvas>
  <Story name="minor/primary/icon">
    <>
      <MinorButton mt={2} ml={2} buttonType="primary"  iconType="print">
        Medium
      </MinorButton>
      <MinorButton
        mt={2}
        buttonType="primary"
        destructive
        iconType="delete"
        iconPosition="after"
        ml={2}
      >
        Medium
      </MinorButton>
      <MinorButton
        mt={2}
        buttonType="primary"
        disabled
        iconType="print"
        iconPosition="after"
        ml={2}
      >
        Medium
      </MinorButton>
    </>
  </Story>
</Canvas>

Primary buttons can be `fullWidth`.

<Canvas>
  <Story name="minor/primary/full-width">
    <MinorButton mt={2} buttonType="primary"  fullWidth>
      Full Width
    </MinorButton>
  </Story>
</Canvas>

Primary buttons can be set into `noWrap` mode to prevent text wrapping.

<Canvas>
  <Story name="minor/primary/noWrap">
    <MinorButton mt={2} ml={2} buttonType="primary"  noWrap>
      Long button text
    </MinorButton>
  </Story>
</Canvas>

### Minor Secondary Buttons

Less prominent, there can be multiple secondary buttons on a page.
Secondary buttons are transparent, rather than white.

<Canvas>
  <Story name="minor/secondary">
    <>
      <MinorButton mt={2} size="small"  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Secondary buttons can be destructive.

<Canvas>
  <Story name="minor/secondary/destructive">
    <>
      <MinorButton mt={2} destructive size="small"  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} destructive ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} destructive size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Secondary buttons can be disabled.

<Canvas>
  <Story name="minor/secondary/disabled">
    <>
      <MinorButton mt={2} size="small" disabled  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} disabled ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} size="large" disabled ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Secondary buttons can have an icon positioned before or after the text.

<Canvas>
  <Story name="minor/secondary/icon">
    <>
      <MinorButton mt={2} iconType="print"  ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} destructive iconType="delete" iconPosition="after" ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} disabled iconType="print" iconPosition="after" ml={2}>
        Minor
      </MinorButton>
    </>
  </Story>
</Canvas>

Secondary buttons can be `fullWidth`.

<Canvas>
  <Story name="minor/secondary/full-width">
    <MinorButton mt={2} buttonType="secondary"  fullWidth>
      Full Width
    </MinorButton>
  </Story>
</Canvas>

Minor Secondary buttons can be set into `noWrap` mode to prevent text wrapping.

<Canvas>
  <Story name="minor/secondary/noWrap" parameters={{ chromatic: { disable: true } }}>
    <MinorButton mt={2} buttonType="secondary"  noWrap>
      Long button text
    </MinorButton>
  </Story>
</Canvas>

### Minor Tertiary Buttons

Minor Tertiary Buttons or ‘ghost’ buttons are used for reversing actions, like ‘Cancel’ or ‘Back’.

<Canvas>
  <Story name="minor/tertiary">
    <>
      <MinorButton mt={2} buttonType="tertiary" size="small"  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Tertiary buttons can be destructive.

<Canvas>
  <Story name="minor/tertiary/destructive">
    <>
      <MinorButton mt={2} buttonType="tertiary" destructive size="small"  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" destructive ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" destructive size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Tertiary buttons can be disabled.

<Canvas>
  <Story name="minor/tertiary/disabled">
    <>
      <MinorButton mt={2} buttonType="tertiary" disabled size="small"  ml={2}>
        Small
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" disabled ml={2}>
        Medium
      </MinorButton>
      <MinorButton mt={2} buttonType="tertiary" disabled size="large" ml={2}>
        Large
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Tertiary buttons can have an icon positioned before or after the text.

<Canvas>
  <Story name="minor/tertiary/icon">
    <>
      <MinorButton mt={2} buttonType="tertiary" iconType="print"  ml={2}>
        Medium
      </MinorButton>
      <MinorButton
        mt={2}
        buttonType="tertiary"
        destructive
        iconType="delete"
        iconPosition="after"
        ml={2}
      >
        Medium
      </MinorButton>
      <MinorButton
        mt={2}
        buttonType="tertiary"
        disabled
        iconType="print"
        iconPosition="after"
        ml={2}
      >
        Medium
      </MinorButton>
    </>
  </Story>
</Canvas>

Minor Tertiary buttons can be `fullWidth`.

<Canvas>
  <Story name="minor/tertiary/full-width">
    <MinorButton mt={2} buttonType="tertiary"  fullWidth>
      Full Width
    </MinorButton>
  </Story>
</Canvas>

Tertiary buttons can be set to `noWrap` mode to prevent text wrapping.

<Canvas>
  <Story name="minor/tertiary/noWrap" parameters={{ chromatic: { disable: true } }}>
    <MinorButton mt={2} buttonType="tertiary"  noWrap>
      Long button text
    </MinorButton>
  </Story>
</Canvas>

### Icon Only Button

Buttons can be rendered with just an icon.

<Canvas>
  <Story name="minor/icon only button">
    <>
      <MinorButton
        mt={2}
        ml={2}
        buttonType="primary"
        size="small"
        iconType="bin"
        aria-label="Delete"
      />
      <MinorButton mt={2} destructive ml={2} iconType="bin" aria-label="Delete" />
      <MinorButton
        mt={2}
        disabled
        size="large"
        ml={2}
        iconType="bin"
        aria-label="Delete"
      />
    </>
  </Story>
</Canvas>

Icon only buttons can also display a tooltip message when the icon is hovered over. This can be acheived by passing a string to the `iconTooltipMessage` prop.

<Canvas>
  <Story
    name="minor/icon only button with tooltip"
    parameters={{ chromatic: { disable: true } }}
  >
    <>
      <MinorButton
        mt={2}
        ml={2}
        buttonType="primary"
        size="small"
        iconType="bin"
        iconTooltipMessage="This is a tooltip"
        aria-label="Delete"
      />
      <MinorButton
        destructive
        mt={2}
        ml={2}
        iconType="bin"
        iconTooltipMessage="This is a tooltip"
        aria-label="Delete"
      />
      <MinorButton
        mt={2}
        disabled
        size="large"
        ml={2}
        iconType="bin"
        iconTooltipMessage="This is a tooltip"
        aria-label="Delete"
      />
    </>
  </Story>
</Canvas>


## Props

Options shared between all of the above types of buttons. When setting padding we recommend using either the `p`, `py`
or `px` props to ensure the spacing within the button is applied evenly.

### Button

<StyledSystemProps
  of={Button}
  spacing
  defaults={{ pt: "1px", pb: "1px", px: "24px" }}
  noHeader
/>

