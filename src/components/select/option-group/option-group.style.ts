import styled from "styled-components";

const StyledOptionGroup = styled.li`
  box-sizing: border-box;
  height: 100%;
  width: 100%;
  padding-left: var(--spacing200);
  padding-top: var(--spacing200);
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export default StyledOptionGroup;
