import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";

import * as DtStories from "./dt/dt.stories";
import * as DdStories from "./dd/dd.stories";
import * as DefinitionListStories from "./definition-list.stories";

<Meta of={DefinitionListStories} />

# Definition List

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Props](#props)

## Quick Start

```javascript
import { Dl, Dt, Dd } from "carbon-react/lib/components/definition-list";
```

## Examples

### Horizontal pairs

<Canvas of={DefinitionListStories.Horizontal} />

### Vertical pairs

Set `asSingleColumn` to render terms above their descriptions.

<Canvas of={DefinitionListStories.Vertical} />

### With dividers

<Canvas of={DefinitionListStories.WithDividers} />

### Spacing

`spacing="small"` applies 4px of top and bottom padding to each pair, producing 8px between adjacent pair content. The default `spacing="medium"` applies 12px per side, producing 24px between adjacent pair content.

<Canvas of={DefinitionListStories.Spacing} />

### Multiple descriptions

Each term can have one or more native `<Dd />` descriptions.

<Canvas of={DefinitionListStories.MultipleDescriptions} />

Each `<Dd />` supports its own `rightChildren` independently, so multiple descriptions under one term can each have their own supplementary element without needing to compose with `Box`.

<Canvas of={DefinitionListStories.MultipleDescriptionsWithRightChildren} />

### With right children

Use the `rightChildren` prop on `Dd` to render a supplementary element, such as a Pill or Link, aligned to the right of the description.

<Canvas of={DefinitionListStories.WithRightChildren} />

## Props

### Dl

<ArgTypes of={DefinitionListStories} />

### Dt

<ArgTypes of={DtStories} />

### Dd

<ArgTypes of={DdStories} />
