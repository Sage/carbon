import colours from 'style-config/colours';

export default {
  base: {
    backgroundColor: colours.red,
    border: 0,
    borderRadius: 4,
    color: 'white',
    padding: '1.5em',
  },

  input: {
    backgroundColor: '#DDD'
  }
};
