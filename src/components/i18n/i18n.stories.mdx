import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import i18n from 'i18n-js';
import I18nComponent from './i18n.js';
import DeprecationWarning from '../../__internal__/DeprecationWarning';

<Meta title="i18nComponent" parameters={{ info: { disable: true }}} />

<DeprecationWarning>
  It is on our roadmap to remove our I18n implementation
  and provide localisation for our components out of the box.
  If you are adding localisation to your application we recommend you use a dedicated solution such as react-i18next
</DeprecationWarning>

# I18nComponent (Internationalization)
Displays a translation from an i18n file.

Internationalization (i18n) means apps can be localized for languages and cultures easily.
This component displays a single text translation.
You can use this component with any other component that displays text.

## Quick Start
Import `I18nComponent` into the project.

```jsx
import I18n from "carbon-react/lib/components/i18n";

const MyComponent = () => (
  <I18nComponent scope='Foo'/>
);
```

### Default
<Preview>  
  <Story name="default">
    {() => {        
      i18n.translations.en['my'] = {
      example: '# My __example__ translation.'
      }; 
      return (
      <div style={{margin: '16px'}}> 
        <I18nComponent scope='my.example'/>
      </div>
      )     
    }}
  </Story>
  <Story name="markdown">
    {() => {        
      i18n.translations.en['my'] = {
      example: '# My __example__ translation.'
      }; 
      return (
      <div style={{}}> 
        <I18nComponent markdown={true} inline={false} scope='my.example'/>
      </div>
      )         
    }} 
  </Story>
  <Story name="inline">
    {() => {        
      i18n.translations.en['my'] = {
      example: '# My __example__ translation.'
      }; 
      return (
      <div style={{margin: '16px'}}> 
        <I18nComponent markdown={true} inline={true} scope='my.example'/>
      </div>
      )         
    }} 
  </Story>
</Preview>

### Markdown
<Preview>
  <Story name="with Markdown" parameters={{ chromatic: { disable: true }}}>
    {() => {
        i18n.translations.en['my'] = {
        example: '# My __example__ translation.'
      }; 
      return (
        <I18nComponent markdown={true} inline={false} scope='my.example'/>
      )         
    }}
  </Story>
</Preview>

### Inline
<Preview>
  <Story name="with Inline" parameters={{ chromatic: { disable: true }}}>
    {() => {
        i18n.translations.en['my'] = {
        example: '# My __example__ translation.'
      }; 
      return (
        <I18nComponent markdown inline scope='my.example'/>
      )         
    }}
  </Story>
</Preview>

## Props
<Props of={ I18nComponent } />