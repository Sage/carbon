import { Meta, ArgTypes, Canvas } from "@storybook/blocks";

import * as FieldsetStories from "./fieldset.stories";

<Meta of={FieldsetStories} />

# Fieldset

This component can be used within a [Form](../?path=/docs/form--docs) component to group related fields together. 
It will render a `<fieldset>` element with a `<legend>` element to provide a title for the group of fields.

## Contents

- [Quick Start](#quick-start)
- [Examples](#examples)
- [Props](#props)

## Quick Start

```javascript
import Fieldset from "carbon-react/lib/components/fieldset";
```

## Examples

### Default

Note that this example is using the `validationRedesignOptIn` flag and is wrapped within a `Form`, which will provide the spacing for the inputs within the Fieldset.
The legacy layout for the `Fieldset` component is still available as seen in the Validation examples below. 

<Canvas of={FieldsetStories.Default} />

### With `fieldSpacing`

You can use the `fieldSpacing` prop on Form to adjust the spacing between inputs.

**Note:** This example is using the `validationRedesignOptIn` flag.  

<Canvas of={FieldsetStories.InFormFieldSpacing} />

### Required

The `required` prop will add an asterisk to the legend and set all fields within the Fieldset to be required.

**Note:** This example is using the `validationRedesignOptIn` flag.

<Canvas of={FieldsetStories.Required} />

### Optional

**Note:** This example is using the `validationRedesignOptIn` flag.

<Canvas of={FieldsetStories.IsOptional} />

### New Validation

The following example uses the new validation pattern that is available by setting the `validationRedesignOptIn` flag on the `CarbonProvider` to true.

<Canvas of={FieldsetStories.NewValidation} />

### Validations

#### As a string

<Canvas of={FieldsetStories.ValidationsStringComponent} />

#### As a string, displayed on label

<Canvas of={FieldsetStories.ValidationsStringLabel} />

#### As a boolean

<Canvas of={FieldsetStories.ValidationsBoolean} />

## Props

### Fieldset

<ArgTypes of={FieldsetStories} />
