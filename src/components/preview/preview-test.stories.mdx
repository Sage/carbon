import { Meta, Story, Canvas } from "@storybook/addon-docs";
import specialCharacters from "../../../.storybook/utils/argTypes/specialCharacters";
import Preview from "./preview.component";

<Meta
  title="Preview/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
  }}
  argTypes={{
    childrenSpecialCharacters: specialCharacters,
  }}
/>

export const PreviewStory = ({
  children,
  childrenSpecialCharacters,
  ...args
}) => <Preview children={children || childrenSpecialCharacters} {...args} />;

# Alert

### Default

<Canvas>
  <Story
    name="default"
    args={{
      children: "Text rendered as children component.",
      childrenSpecialCharacters: undefined,
      height: "",
      lines: Preview.defaultProps.lines,
      loading: true,
      width: "",
    }}
  >
    {PreviewStory.bind({})}
  </Story>
</Canvas>
