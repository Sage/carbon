import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";

import TableWrapper from "./table-story-helpers/table-story-wrapper.component";
import { Table } from ".";

<Meta
  title="Table/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
    knobs: {
      disable: true,
    },
  }}
  argTypes={{
    sortOrder: {
      options: ["", "asc", "desc"],
      control: {
        type: "select",
      },
    },
    sortedColumn: {
      options: ["", "name", "code"],
      control: {
        type: "select",
      },
    },
    tableTheme: {
      options: ["primary", "secondary", "tertiary"],
      control: {
        type: "select",
      },
    },
    size: {
      options: ["compact", "small", "medium", "large"],
      control: {
        type: "select",
      },
    },
    inputType: {
      options: ["textbox", "textarea", "date"],
      control: {
        type: "select",
      },
    },
  }}
/>

export const TableStory = (args) => <TableWrapper {...args} />;

# Table

### Default

export const commonArgs = {
  sortOrder: "",
  sortedColumn: "",
  highlightable: false,
  selectable: false,
  isPassiveData: false,
  shrink: false,
  caption: "Country and Country Codes",
  totalRecords: 193,
  pageSize: 10,
  paginate: false,
  showPageSizeSelection: false,
  theme: Table.defaultProps.theme,
  size: Table.defaultProps.size,
  isZebra: false,
};

<Preview>
  <Story name="default" args={commonArgs}>
    {TableStory.bind({})}
  </Story>
</Preview>

<Preview>
  <Story name="default with inputs" args={{
      ...commonArgs,
      inputType: "textbox",
      
  }}>
    {TableStory.bind({})}
  </Story>
</Preview>