export default () => `
  -webkit-box-shadow:
    inset 0 0 0 var(--global-borderwidth-s) var(--focus-borderalt);
  box-shadow:
    inset 0 0 0 var(--global-borderwidth-s) var(--focus-borderalt);

  outline: transparent 3px solid;
`;
